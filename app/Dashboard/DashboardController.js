'use strict';
var DashboardController = function($scope, $location,authService) {

    
    //pyjacOMSService.newOrder("Buy");
    
    /*
    var connection = $.hubConnection(endpoint);

     var proxy = connection.createHubProxy(hub);

     proxy.on('hello', function (response) {

         $scope.$apply(function () {

             $scope.response = response;

         });

     });

     connection.start()

         .done(function () {

             console.log('signlar connection established');

         })

         .fail(function (error) {

             console.log(error);

     });*/
};
module.exports = DashboardController;